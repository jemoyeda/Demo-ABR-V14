<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Vista formulario para libros-->
    <record id="libros" model="ir.ui.view">
        <field name="name">libro.form.view</field>
        <field name="model">libros</field>
        <field name="arch" type="xml">
            <form string="libro">
                <sheet>
                    <group>
                        <group>
                            <field name="imagen" widget="image" class="oi_avatar"/> <!-- Agregar widget y class para que aparezca la imagen en el formulario-->
                            <field name="name"/>
                            <field name="editorial"/>
                            <field name="isbn"/>
                            <field name="autor_id"/>
                            <field name="last_name"/>
                        </group>
                    </group>
                </sheet>
            </form>
        </field>
    </record>

    <!--Vista Tree para libros-->
    <record id="view_tree_libros" model="ir.ui.view">
        <field name="name">libros.tree.view</field>
        <field name="model">libros</field>
        <field name="arch" type="xml">
            <tree string="libros">
                <field name="name"/>
                <field name="editorial"/>
                <field name="isbn"/>
                <field name="autor_id" optional="hide"/>
            </tree>
        </field>
    </record>


    <!--Vista Kanban para libros
    <record id="libros_kanban_view" model="ir.ui.view">
        <field name="name">libro.kanban.view</field>
        <field name="model">libros</field>
        <field name="arch" type="xml">
            <kanban>
                <field name="id"/>
                <field name="name"/>
                <field name="editorial"/>
                <field name="isbn"/>
                <field name="autor_id"/>
                <!--<templates>
                    <t t-name="kanban-box">
                        <div class="es_kanban_global_click">
                            <div class="o_kanban_image">
                                <img t-att-src="kanban_image('libros', 'imagen', 'record.id.raw.value')" alt="Imagen"/>
                            </div>
                            <div class="es_kanban_detalles">
                                <div class="o_kanban_record_top"/>
                                    <div calss="o_kanban_record_headings">
                                        <strong class="o_kanban_record_title or_partner_headings">
                                            <field name="name"/>
                                        </strong>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </t>
                </templates>-->
            </kanban>
        </field>
    </record>
</odoo>-->
